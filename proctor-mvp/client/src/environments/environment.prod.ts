export const environment = {
  production: true,
  // API URL - will be replaced by build script based on NETLIFY_API_URL environment variable
  // Set NETLIFY_API_URL in Netlify dashboard: Site settings > Environment variables
  // Example: https://your-backend.railway.app (without /api suffix)
  apiUrl: '/api'
};
